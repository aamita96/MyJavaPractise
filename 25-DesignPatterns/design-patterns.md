## 💡 Rule of thumb for when to use:

- **Singleton:** Only one instance needed globally

- **Factory:** Multiple types of objects with same interface

- **Builder:** Object has many optional fields, long constructors would be messy